{{- if .Values.crd.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.21.0
  name: fabricnetworks.fabricops.io
spec:
  group: fabricops.io
  names:
    kind: FabricNetwork
    listKind: FabricNetworkList
    plural: fabricnetworks
    singular: fabricnetwork
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FabricNetwork is the Schema for the fabricnetworks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of FabricNetwork
            properties:
              chaincodes:
                items:
                  properties:
                    ccaas:
                      description: |-
                        CCAAS describes the Kubernetes Chaincode-as-a-Service workload and
                        connection package settings for this chaincode.
                      properties:
                        containerPort:
                          default: 7052
                          description: ContainerPort is the port exposed by the chaincode
                            server container.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        dialTimeout:
                          default: 10s
                          description: DialTimeout is written into Fabric CCaaS connection.json.
                          pattern: ^([0-9]+(ms|s|m|h))+$
                          type: string
                        env:
                          description: Env adds extra environment variables to the
                            chaincode server container.
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                    - key
                                    - path
                                    - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        imagePullPolicy:
                          default: IfNotPresent
                          description: ImagePullPolicy controls when Kubernetes pulls
                            the chaincode image.
                          enum:
                          - Always
                          - Never
                          - IfNotPresent
                          type: string
                        replicas:
                          default: 1
                          description: Replicas is the number of chaincode server
                            pods behind the Service.
                          format: int32
                          minimum: 1
                          type: integer
                        resources:
                          description: Resources controls chaincode server container
                            resource requests/limits.
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This field depends on the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        servicePort:
                          default: 7052
                          description: ServicePort is the Kubernetes Service port
                            peers use from connection.json.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                      type: object
                    channel:
                      maxLength: 249
                      minLength: 1
                      pattern: ^[a-z0-9]([a-z0-9.-]*[a-z0-9])?$
                      type: string
                    couchdbIndexes:
                      description: |-
                        CouchDBIndexes declares JSON CouchDB indexes that should be packaged
                        with this chaincode. Public indexes are rendered under
                        metadata/META-INF/statedb/couchdb/indexes; collection-scoped indexes are
                        rendered under metadata/META-INF/statedb/couchdb/collections/<collection>/indexes.
                      items:
                        properties:
                          collection:
                            description: |-
                              Collection optionally scopes the index to a private data collection.
                              When empty, the index is packaged for the chaincode public state.
                            maxLength: 64
                            pattern: ^[A-Za-z0-9][A-Za-z0-9_-]*$
                            type: string
                          designDocument:
                            description: DesignDocument optionally sets the CouchDB
                              design document (`ddoc`).
                            maxLength: 128
                            pattern: ^[A-Za-z0-9][A-Za-z0-9_.-]*$
                            type: string
                          fields:
                            description: Fields is the ordered list of JSON document
                              fields indexed by CouchDB.
                            items:
                              type: string
                            maxItems: 32
                            minItems: 1
                            type: array
                          name:
                            description: Name is the CouchDB index name and the basis
                              for the packaged file name.
                            maxLength: 128
                            minLength: 1
                            pattern: ^[A-Za-z0-9][A-Za-z0-9_.-]*$
                            type: string
                        required:
                        - fields
                        - name
                        type: object
                      type: array
                    endorsementPolicy:
                      description: |-
                        EndorsementPolicy is passed as a Fabric signature policy during approve
                        and commit. When empty, the operator will derive a channel-org policy.
                      maxLength: 512
                      type: string
                    image:
                      maxLength: 512
                      minLength: 1
                      type: string
                    initRequired:
                      description: InitRequired controls the Fabric lifecycle --init-required
                        flag.
                      type: boolean
                    name:
                      maxLength: 63
                      minLength: 1
                      pattern: ^[A-Za-z0-9]([A-Za-z0-9_.-]*[A-Za-z0-9])?$
                      type: string
                    packageLabel:
                      description: |-
                        PackageLabel overrides the default lifecycle package label. When empty,
                        the operator uses <channel>_<name>_<version>.
                      maxLength: 128
                      minLength: 1
                      pattern: ^[A-Za-z0-9]([A-Za-z0-9_.-]*[A-Za-z0-9])?$
                      type: string
                    privateData:
                      description: |-
                        PrivateData declares explicit Fabric private data collections for this
                        chaincode definition. The operator renders these entries to the Fabric
                        collection config JSON used during approve and commit.
                      items:
                        properties:
                          blockToLive:
                            description: |-
                              BlockToLive controls private data purging by block age. Zero keeps data
                              indefinitely.
                            format: int64
                            minimum: 0
                            type: integer
                          endorsementPolicy:
                            description: |-
                              EndorsementPolicy optionally overrides the chaincode endorsement policy for
                              this collection.
                            properties:
                              channelConfigPolicy:
                                description: ChannelConfigPolicy references a policy
                                  from the channel configuration.
                                maxLength: 256
                                type: string
                              signaturePolicy:
                                description: |-
                                  SignaturePolicy is a Fabric signature policy, for example
                                  AND('BankAMSP.member','BankBMSP.member').
                                maxLength: 512
                                type: string
                            type: object
                          maxPeerCount:
                            description: |-
                              MaxPeerCount is the maximum number of authorized peers each endorsing peer
                              attempts to disseminate private data to.
                            format: int32
                            maximum: 50
                            minimum: 0
                            type: integer
                          memberOnlyRead:
                            description: |-
                              MemberOnlyRead asks peers to enforce that only collection member
                              organizations can read collection data.
                            type: boolean
                          memberOnlyWrite:
                            description: |-
                              MemberOnlyWrite asks peers to enforce that only collection member
                              organizations can write collection data.
                            type: boolean
                          name:
                            description: Name is the Fabric private data collection
                              name.
                            maxLength: 64
                            minLength: 1
                            pattern: ^[A-Za-z0-9][A-Za-z0-9_-]*$
                            type: string
                          orgNames:
                            description: |-
                              OrgNames lists the channel organizations allowed to store collection data.
                              When Policy is empty, the operator derives an OR('<MSP>.member', ...)
                              policy from this list.
                            items:
                              type: string
                            minItems: 1
                            type: array
                          policy:
                            description: Policy overrides the derived Fabric collection
                              distribution policy.
                            maxLength: 512
                            type: string
                          requiredPeerCount:
                            description: |-
                              RequiredPeerCount is the minimum number of authorized peers that must
                              receive private data before endorsement succeeds.
                            format: int32
                            maximum: 50
                            minimum: 0
                            type: integer
                        required:
                        - name
                        - orgNames
                        type: object
                      type: array
                    sequence:
                      default: 1
                      description: |-
                        Sequence is the Fabric lifecycle definition sequence to use when
                        approving and committing the chaincode.
                      format: int32
                      minimum: 1
                      type: integer
                    version:
                      maxLength: 128
                      minLength: 1
                      type: string
                  required:
                  - channel
                  - image
                  - name
                  - version
                  type: object
                type: array
              channels:
                items:
                  properties:
                    name:
                      maxLength: 249
                      minLength: 1
                      pattern: ^[a-z0-9]([a-z0-9.-]*[a-z0-9])?$
                      type: string
                    orgs:
                      items:
                        properties:
                          name:
                            maxLength: 63
                            minLength: 1
                            pattern: ^[A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?$
                            type: string
                          peers:
                            items:
                              type: string
                            minItems: 1
                            type: array
                        required:
                        - name
                        - peers
                        type: object
                      minItems: 1
                      type: array
                  required:
                  - name
                  - orgs
                  type: object
                type: array
              global:
                properties:
                  fabricVersion:
                    maxLength: 64
                    minLength: 1
                    pattern: ^[A-Za-z0-9][A-Za-z0-9._-]*$
                    type: string
                  networkPolicy:
                    properties:
                      enabled:
                        default: false
                        description: |-
                          Enabled creates org boundary NetworkPolicies for FabricOps-managed pods.
                          The cluster CNI must support Kubernetes NetworkPolicy enforcement.
                        type: boolean
                    type: object
                  observability:
                    properties:
                      serviceMonitor:
                        description: |-
                          ServiceMonitor controls optional Prometheus Operator ServiceMonitor
                          output for Fabric operations endpoint Services.
                        properties:
                          enabled:
                            default: false
                            description: |-
                              Enabled creates one ServiceMonitor per generated org namespace. The
                              monitoring.coreos.com/v1 ServiceMonitor CRD must already exist.
                            type: boolean
                          interval:
                            default: 30s
                            description: |-
                              Interval configures how often Prometheus scrapes Fabric operations
                              endpoints.
                            pattern: ^([0-9]+(ms|s|m|h))+$
                            type: string
                          labels:
                            additionalProperties:
                              type: string
                            description: |-
                              Labels adds metadata labels to generated ServiceMonitors. Use this for
                              Prometheus release selectors such as release=<name>.
                            type: object
                          scrapeTimeout:
                            default: 10s
                            description: ScrapeTimeout configures the Prometheus scrape
                              timeout.
                            pattern: ^([0-9]+(ms|s|m|h))+$
                            type: string
                        type: object
                    type: object
                  storage:
                    properties:
                      ca:
                        properties:
                          size:
                            description: Size is the persistent volume request for
                              each component instance.
                            maxLength: 32
                            minLength: 1
                            pattern: ^[0-9]+(Ei|Pi|Ti|Gi|Mi|Ki|E|P|T|G|M|K)?$
                            type: string
                          storageClassName:
                            description: |-
                              StorageClassName selects the Kubernetes StorageClass for component PVCs.
                              Omit this field to use the cluster default StorageClass. Set it to an
                              empty string to disable dynamic provisioning for clusters that rely on
                              pre-bound persistent volumes.
                            type: string
                        type: object
                      orderer:
                        properties:
                          size:
                            description: Size is the persistent volume request for
                              each component instance.
                            maxLength: 32
                            minLength: 1
                            pattern: ^[0-9]+(Ei|Pi|Ti|Gi|Mi|Ki|E|P|T|G|M|K)?$
                            type: string
                          storageClassName:
                            description: |-
                              StorageClassName selects the Kubernetes StorageClass for component PVCs.
                              Omit this field to use the cluster default StorageClass. Set it to an
                              empty string to disable dynamic provisioning for clusters that rely on
                              pre-bound persistent volumes.
                            type: string
                        type: object
                      peer:
                        properties:
                          size:
                            description: Size is the persistent volume request for
                              each component instance.
                            maxLength: 32
                            minLength: 1
                            pattern: ^[0-9]+(Ei|Pi|Ti|Gi|Mi|Ki|E|P|T|G|M|K)?$
                            type: string
                          storageClassName:
                            description: |-
                              StorageClassName selects the Kubernetes StorageClass for component PVCs.
                              Omit this field to use the cluster default StorageClass. Set it to an
                              empty string to disable dynamic provisioning for clusters that rely on
                              pre-bound persistent volumes.
                            type: string
                        type: object
                    type: object
                  tls:
                    type: boolean
                required:
                - fabricVersion
                - tls
                type: object
              orgs:
                items:
                  properties:
                    ca:
                      properties:
                        db:
                          maxLength: 32
                          minLength: 1
                          pattern: ^[A-Za-z0-9_-]+$
                          type: string
                      required:
                      - db
                      type: object
                    orderers:
                      items:
                        properties:
                          groupName:
                            maxLength: 63
                            minLength: 1
                            pattern: ^[A-Za-z0-9]([A-Za-z0-9_.-]*[A-Za-z0-9])?$
                            type: string
                          instances:
                            maximum: 50
                            minimum: 1
                            type: integer
                          prefix:
                            maxLength: 50
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                          type:
                            maxLength: 32
                            minLength: 1
                            pattern: ^[A-Za-z0-9_-]+$
                            type: string
                        required:
                        - groupName
                        - instances
                        - prefix
                        - type
                        type: object
                      type: array
                    organization:
                      properties:
                        domain:
                          maxLength: 253
                          minLength: 3
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?\.)+[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        mspName:
                          maxLength: 64
                          minLength: 1
                          pattern: ^[A-Za-z][A-Za-z0-9]*$
                          type: string
                        name:
                          maxLength: 63
                          minLength: 1
                          pattern: ^[A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?$
                          type: string
                      required:
                      - domain
                      - mspName
                      - name
                      type: object
                    peer:
                      properties:
                        db:
                          maxLength: 32
                          minLength: 1
                          pattern: ^[A-Za-z0-9_-]+$
                          type: string
                        instances:
                          maximum: 50
                          minimum: 1
                          type: integer
                        prefix:
                          maxLength: 50
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                      required:
                      - db
                      - instances
                      - prefix
                      type: object
                  required:
                  - ca
                  - organization
                  type: object
                minItems: 1
                type: array
            required:
            - chaincodes
            - channels
            - global
            - orgs
            type: object
          status:
            description: status defines the observed state of FabricNetwork
            properties:
              chaincodeStatus:
                items:
                  properties:
                    approved:
                      properties:
                        desired:
                          format: int32
                          type: integer
                        ready:
                          format: int32
                          type: integer
                      required:
                      - desired
                      - ready
                      type: object
                    approvedReady:
                      type: boolean
                    channel:
                      type: string
                    collectionConfigHash:
                      type: string
                    collectionConfigMap:
                      type: string
                    commitJobName:
                      type: string
                    committed:
                      type: boolean
                    installed:
                      properties:
                        desired:
                          format: int32
                          type: integer
                        ready:
                          format: int32
                          type: integer
                      required:
                      - desired
                      - ready
                      type: object
                    installedReady:
                      type: boolean
                    message:
                      type: string
                    name:
                      type: string
                    packageLabel:
                      type: string
                    packageMetadata:
                      properties:
                        desired:
                          format: int32
                          type: integer
                        ready:
                          format: int32
                          type: integer
                      required:
                      - desired
                      - ready
                      type: object
                    packageMetadataReady:
                      type: boolean
                    ready:
                      type: boolean
                    sequence:
                      format: int32
                      type: integer
                    targets:
                      items:
                        properties:
                          address:
                            type: string
                          approveJobName:
                            type: string
                          approved:
                            type: boolean
                          chaincodeID:
                            type: string
                          installJobName:
                            type: string
                          installed:
                            type: boolean
                          message:
                            type: string
                          namespace:
                            type: string
                          orgName:
                            type: string
                          packageConfigMapName:
                            type: string
                          packageID:
                            type: string
                          packageIDConfigMapName:
                            type: string
                          packageMetadataReady:
                            type: boolean
                          peerName:
                            type: string
                          serviceName:
                            type: string
                          workload:
                            properties:
                              desired:
                                format: int32
                                type: integer
                              ready:
                                format: int32
                                type: integer
                            required:
                            - desired
                            - ready
                            type: object
                          workloadName:
                            type: string
                          workloadReady:
                            type: boolean
                        required:
                        - approved
                        - installed
                        - orgName
                        - packageMetadataReady
                        - peerName
                        - workloadReady
                        type: object
                      type: array
                    version:
                      type: string
                    workloads:
                      properties:
                        desired:
                          format: int32
                          type: integer
                        ready:
                          format: int32
                          type: integer
                      required:
                      - desired
                      - ready
                      type: object
                    workloadsReady:
                      type: boolean
                  required:
                  - approvedReady
                  - channel
                  - committed
                  - installedReady
                  - name
                  - packageMetadataReady
                  - ready
                  - version
                  - workloadsReady
                  type: object
                type: array
              channelStatus:
                items:
                  properties:
                    blockConfigMapName:
                      type: string
                    blockReady:
                      type: boolean
                    configMapName:
                      type: string
                    configReady:
                      type: boolean
                    message:
                      type: string
                    name:
                      type: string
                    orderers:
                      properties:
                        desired:
                          format: int32
                          type: integer
                        ready:
                          format: int32
                          type: integer
                      required:
                      - desired
                      - ready
                      type: object
                    orgs:
                      items:
                        properties:
                          message:
                            type: string
                          mspName:
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                          peerNames:
                            items:
                              type: string
                            type: array
                          peers:
                            properties:
                              desired:
                                format: int32
                                type: integer
                              ready:
                                format: int32
                                type: integer
                            required:
                            - desired
                            - ready
                            type: object
                          ready:
                            type: boolean
                        required:
                        - name
                        - ready
                        type: object
                      type: array
                    peers:
                      properties:
                        desired:
                          format: int32
                          type: integer
                        ready:
                          format: int32
                          type: integer
                      required:
                      - desired
                      - ready
                      type: object
                    ready:
                      type: boolean
                  required:
                  - blockReady
                  - configReady
                  - name
                  - ready
                  type: object
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                type: string
              orgStatus:
                items:
                  properties:
                    caEndpoint:
                      description: CAEndpoint is the in-cluster Fabric CA endpoint
                        for this org.
                      type: string
                    caReady:
                      type: boolean
                    connectionProfileConfigMapName:
                      description: |-
                        ConnectionProfileConfigMapName points application clients at the
                        generated Fabric connection profile ConfigMap for this org.
                      type: string
                    identityError:
                      type: string
                    identityReady:
                      type: boolean
                    name:
                      type: string
                    namespace:
                      type: string
                    ordererEndpoints:
                      description: |-
                        OrdererEndpoints lists in-cluster client, admin, and operations
                        endpoints for desired orderer workloads in this org.
                      items:
                        properties:
                          adminAddress:
                            type: string
                          clientAddress:
                            type: string
                          name:
                            type: string
                          operationsAddress:
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                    orderers:
                      properties:
                        desired:
                          format: int32
                          type: integer
                        ready:
                          format: int32
                          type: integer
                      required:
                      - desired
                      - ready
                      type: object
                    orderersReady:
                      type: boolean
                    peerEndpoints:
                      description: |-
                        PeerEndpoints lists in-cluster client, chaincode, and operations
                        endpoints for desired peer workloads in this org.
                      items:
                        properties:
                          address:
                            type: string
                          chaincodeAddress:
                            type: string
                          name:
                            type: string
                          operationsAddress:
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                    peers:
                      properties:
                        desired:
                          format: int32
                          type: integer
                        ready:
                          format: int32
                          type: integer
                      required:
                      - desired
                      - ready
                      type: object
                    peersReady:
                      type: boolean
                    ready:
                      type: boolean
                  required:
                  - caReady
                  - identityReady
                  - name
                  - orderersReady
                  - peersReady
                  - ready
                  type: object
                type: array
              phase:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
